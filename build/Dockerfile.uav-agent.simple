FROM alpine:latest

# 安装运行时依赖（网络工具）
RUN apk --no-cache add ca-certificates curl busybox-extras || true

WORKDIR /root/

# 复制预编译的二进制文件
COPY bin/uav-agent .

# 暴露端口
EXPOSE 9090

# 启动命令
CMD ["./uav-agent"]
